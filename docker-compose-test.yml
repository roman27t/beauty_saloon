version: "3.9"

#volumes:
#  pg_data:
#  cache:
#    driver: local

networks:
  app-network-2-test:

services:
  pg_db-2-test:
    image: postgres:15.1
    container_name: pg_name-2-test
    restart: always
    environment:
      POSTGRES_DB: "postgres_test"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      PGDATA: "/var/lib/postgresql/data/pg_data"
    ports:
      - "54320:5432"
    networks:
      - app-network-2-test

  cache-redis-test:
    image: redis:7.0.7-alpine
    restart: always
    ports:
      - '63791:6379'
    command: redis-server --save 20 1 --loglevel warning --requirepass eYVX7EwVmmxKPCDmwMtyKVge8oLd2t81
    networks:
      - app-network-2-test
  app-test:
    build:
      context: .
    depends_on:
      - pg_db-2-test
      - cache-redis-test
    container_name: app-backend-2-test
    command: sh -c "alembic upgrade head && uvicorn main:app --reload --host 0.0.0.0"
#    command: sh -c "alembic upgrade head && python main.py"
    volumes:
      - ./app:/app
    ports:
      - "9000:8000"
    tty: true
    stdin_open: true
    restart: always
    networks:
      - app-network-2-test
    environment:
      DB_URL: "postgresql+asyncpg://postgres:postgres@pg_db-2-test/postgres_test"
      PORT: 9000
      PYTHONPATH: "/app"
      DEBUG: "1"
      REDIS_HOST: cache-redis-test
      REDIS_PORT: 63791
      REDIS_PASSWORD: eYVX7EwVmmxKPCDmwMtyKVge8oLd2t81
